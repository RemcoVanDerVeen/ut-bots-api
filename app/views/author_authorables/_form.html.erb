<%= form_with(model: author_authorable) do |form| %>
  <% if author_authorable.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(author_authorable.errors.count, "error") %> prohibited this author authorable from being saved:</h2>

      <ul>
        <% author_authorable.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
