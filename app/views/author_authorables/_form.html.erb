<%= form_with(model: authorable) do |form| %>
  <% if authorable.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(authorable.errors.count, "error") %> prohibited this authorable from being saved:</h2>

      <ul>
        <% authorable.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
