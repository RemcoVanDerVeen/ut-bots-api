<%= form_with(model: player_model_skin) do |form| %>
  <% if player_model_skin.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(player_model_skin.errors.count, "error") %> prohibited this player_model_skin from being saved:</h2>

      <ul>
        <% player_model_skin.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
