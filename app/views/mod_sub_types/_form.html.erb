<%= form_with(model: mod_sub_type) do |form| %>
  <% if mod_sub_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mod_sub_type.errors.count, "error") %> prohibited this mod_sub_type from being saved:</h2>

      <ul>
        <% mod_sub_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
