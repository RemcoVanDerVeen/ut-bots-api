<%= form_with(model: player_model_skin_face) do |form| %>
  <% if player_model_skin_face.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(player_model_skin_face.errors.count, "error") %> prohibited this player_model_skin_face from being saved:</h2>

      <ul>
        <% player_model_skin_face.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
