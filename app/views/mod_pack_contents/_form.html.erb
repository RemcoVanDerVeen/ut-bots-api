<%= form_with(model: mod_pack_content) do |form| %>
  <% if mod_pack_content.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mod_pack_content.errors.count, "error") %> prohibited this mod_pack_content from being saved:</h2>

      <ul>
        <% mod_pack_content.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
