<%= form_with(model: mod_pack) do |form| %>
  <% if mod_pack.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mod_pack.errors.count, "error") %> prohibited this mod_pack from being saved:</h2>

      <ul>
        <% mod_pack.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
